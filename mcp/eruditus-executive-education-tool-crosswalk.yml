generated: '2026-08-04'
method: derived
source: mcp/eruditus-executive-education-mcp.yml, openapi/eruditus-executive-education-leads-openapi.yml,
  openapi/eruditus-executive-education-programs-openapi.yml
summary: >-
  Binding between the candidate MCP tool surface and the backing REST operations. Emeritus
  ships no MCP server and no GraphQL endpoint, so this crosswalk is a clean 1:1 projection of
  the published REST estate — there are no MCP-only or GraphQL-only capabilities to record and
  no REST operations left unbound. Confidence is high on every row because each tool was
  derived directly from its operation; each tool's real inputSchema is that operation's
  requestBody / parameters in the referenced spec.
surfaces:
  openapi:
  - file: openapi/eruditus-executive-education-leads-openapi.yml
    operations: 1
    gated: false
    note: Generated by API Evangelist from the provider's published HTML reference; endpoint is live and auth-gated (HTTP 401 without a key).
  - file: openapi/eruditus-executive-education-programs-openapi.yml
    operations: 3
    gated: false
    note: Paths and auth published; request parameters and success schemas are NOT published by the provider.
  graphql:
    endpoint: null
    note: No GraphQL surface. https://admissions.emeritus.org/api/v1/graphql and https://emeritus.org/graphql both 404 (probed 2026-08-04).
  mcp:
    url: null
    gated: null
    note: No hosted MCP server found; tools/list could not be called because no endpoint exists.
crosswalk:
- tool: createGenericLead
  category: leads
  rest: [createGenericLead]
  binding: rest
  confidence: high
  note: >-
    One REST operation carries two documented request shapes (B2C and B2B/team, selected by
    inquiring_for). An MCP implementation may prefer to split this into two tools; the input
    contract is the oneOf in the spec's requestBody.
- tool: listPrograms
  category: catalog
  rest: [listPrograms]
  binding: rest
  confidence: high
- tool: listSchools
  category: catalog
  rest: [listSchools]
  binding: rest
  confidence: high
- tool: listLandingPageTemplates
  category: catalog
  rest: [listLandingPageTemplates]
  binding: rest
  confidence: high
mcp_only: []
rest_only: []
coverage:
  tools_named: 4
  tools_bound: 4
  mcp_only: 0
  graphql_only: 0
  rest_operations_total: 4
  rest_operations_with_tool: 4
caveats:
- The three catalog tools inherit no input parameters and no output schema, because the
  provider does not publish them for the Programs API. An agent can call them but cannot be
  told in advance what to send or what comes back.
